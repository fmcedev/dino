using System;
using System.Windows.Forms;

public static class RemoteEntry
{
    [STAThread]
    public static void Main(string[] args)
    {
        // this will pop up a WinForms message box when your host app runs
        MessageBox.Show(
            "✔️ Remote code successfully executed!",
            "Remote Code Test",
            MessageBoxButtons.OK,
            MessageBoxIcon.Information
        );
    }
}
